Need to review data formats throughout program- too many conversions to different types when not needed.

Specifically, check Fetch and format for match data- converting to DataFrames increases complexity instead of formatting the json data directly.